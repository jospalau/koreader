local _ = require("gettext")

return {
    name = "sessioncleaner",
    fullname = _("Session Cleaner"),
    description = _([[Browse books with reading statistics, reconstruct reading sessions, create backups, inspect exact raw rows, and delete unwanted sessions safely from the statistics database.]]),
}
